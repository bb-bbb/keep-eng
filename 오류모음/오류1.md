오류1)

# 🔍 문제 원인과 해결 과정 정리

## 📌 최초 오류

json

```json
{
"detail":"Failed to fetch transcript: type object 'YouTubeTranscriptApi' has no attribute 'get_transcript'"
}
```

---

## 🐛 문제의 근본 원인

### 1️⃣ **잘못된 패키지 버전 사용**

**문제:**

* 코드는 `YouTubeTranscriptApi.get_transcript()` 메서드를 사용하려고 했음
* 하지만 설치된 버전(`0.6.2`)에는 이 메서드가 **존재하지 않음**

**원인:**

* `youtube-transcript-api` 라이브러리의 API가 **버전마다 다름**
* 버전 0.6.x: `list()`, `fetch()` 메서드 사용 (인스턴스 기반)
* 버전 1.x 이상: `get_transcript()` 메서드 사용 (클래스 메서드)

### 2️⃣ **conda 환경 권한 문제**

**문제:**

* conda 환경(`ml_dev`)에 패키지 설치 시도 → 실패
* 시스템 Python 경로에 계속 설치됨
* 결과: 잘못된 패키지 참조

**오류 메시지:**

bash

```bash
Defaulting to user installation because normal site-packages is not writeable
Location: C:\Users\bbibe\AppData\Roaming\Python\Python311\site-packages
```

---

## ✅ 해결 방법

### 1단계: 환경 문제 해결

bash

```bash
# venv 가상환경 새로 생성
python -m venv venv

# 활성화
venv\Scripts\activate

# 올바른 버전 설치
pip install youtube-transcript-api==0.6.2
```

### 2단계: API 사용법 확인

**테스트 코드로 실제 API 확인:**

python

```python
from youtube_transcript_api import YouTubeTranscriptApi

api = YouTubeTranscriptApi()
print(dir(api))# ['fetch', 'list'] 확인
```

**결과:**

* ❌ `get_transcript()` 없음
* ✅ `list()`, `fetch()` 있음

### 3단계: 코드 수정

**기존 코드 (작동 안 함):**

python

```python
# 버전 1.x 방식
raw_transcript = YouTubeTranscriptApi.get_transcript(
    video_id,
    languages=["en"]
)
```

**수정된 코드 (작동함):**

python

```python
# 버전 0.6.x 방식
api = YouTubeTranscriptApi()# 1. 인스턴스 생성
transcript_list = api.list(video_id)# 2. 자막 목록 확인
raw_transcript = api.fetch(video_id,['en'])# 3. 자막 가져오기

# 4. 데이터 변환
return[
{
"text": item.text,# ← .text 속성 접근
"start": item.start,# ← .start 속성 접근
}
for item in raw_transcript
]
```

---

## 🎯 핵심 포인트

| 구분                    | 기존 (오류)          | 수정 (정상)              |
| ----------------------- | -------------------- | ------------------------ |
| **API 호출 방식** | 클래스 메서드        | 인스턴스 메서드          |
| **메서드 이름**   | `get_transcript()` | `list()` + `fetch()` |
| **데이터 접근**   | `item["text"]`     | `item.text`            |
| **설치 환경**     | conda (권한 없음)    | venv (권한 있음)         |

---

## 📚 교훈

1.**라이브러리 버전 확인 필수**: 같은 이름이라도 API가 다를 수 있음
2.**가상환경 권한 체크**: 설치 위치를 항상 확인 (`pip show`)
3.**실제 API 테스트**: `dir()`, `type()`으로 사용 가능한 메서드 확인
4.**공식 문서 확인**: GitHub README에 버전별 사용법 명시되어 있음

---

## 🔧 최종 해결 요약

```
문제: YouTubeTranscriptApi에 get_transcript 속성이 없음
  ↓
원인: 설치된 버전(0.6.2)은 다른 API 사용
  ↓
해결: 
1. venv로 환경 재구성
2. API 구조 파악 (list+ fetch 방식)
3. 코드 수정 (인스턴스 생성 → fetch 호출)
  ↓
결과: ✅ 정상 작동
```
